# M1 sandbox image — intentionally minimal. The real build toolchain (Node/pnpm,
# Playwright, etc.) arrives in M2. M1 only needs a shell to prove containment.
FROM alpine:3.20
CMD ["sleep", "infinity"]
